/* Panic Scanner production billing/account bridge. */
(function(){
  const TK='pswAccessToken';let profile=null;
  function token(){return localStorage.getItem(TK)||''}
  async function recoverSession(){if(token())return token();try{if(!window.supabase){await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2';s.onload=resolve;s.onerror=reject;document.head.appendChild(s)})}const c=window.supabase.createClient('https://xinhpzibmvzqzahcklgy.supabase.co','sb_publishable_YsqF0jHnjrGY2anRaoH9pg_JKqiPqom',{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});const r=await c.auth.getSession();const x=r.data?.session;if(x?.access_token){localStorage.setItem(TK,x.access_token);if(x.refresh_token)localStorage.setItem('pswRefreshToken',x.refresh_token);localStorage.setItem('psLoggedIn','1');if(x.user?.email)localStorage.setItem('psEmail',x.user.email);return x.access_token}}catch(e){}return ''}
  async function api(path,options){await recoverSession();const o=options||{};o.headers=Object.assign({'Content-Type':'application/json'},o.headers||{});if(token())o.headers.Authorization='Bearer '+token();const r=await fetch(path,o);const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Request failed');return d}
  function plan(){if(profile?.is_admin||profile?.is_vip)return 'elite';const p=String(profile?.plan||'standard').toLowerCase();return p==='elite'?'elite':p==='pro'?'pro':'standard'}
  async function refreshProfile(){await recoverSession();if(!token()){profile=null;return}try{const d=await api('/api/account');profile=d.profile||null;if(profile){localStorage.setItem('psEmail',profile.email||'');localStorage.setItem('psPlan',plan());localStorage.setItem('psAdmin',profile.is_admin?'1':'0')}}catch(e){}}
  function patchPlans(){document.querySelectorAll('.plan.pro,.plan.elite').forEach(card=>{card.querySelectorAll('button').forEach(b=>b.remove());card.querySelectorAll('.price').forEach(x=>x.remove())})}
  async function startCheckout(p,interval='monthly'){await recoverSession();if(!token()){if(window.psFinalShowAuth)window.psFinalShowAuth('login');return}try{const d=await api('/api/billing',{method:'POST',body:JSON.stringify({action:'checkout',plan:p,interval})});if(d.url)location.href=d.url;else throw Error('Checkout URL missing.')}catch(e){if(window.toast)toast(e.message)}}
  async function manageBilling(){await recoverSession();if(!token()){if(window.psFinalShowAuth)window.psFinalShowAuth('login');return}try{const d=await api('/api/billing',{method:'POST',body:JSON.stringify({action:'portal'})});if(d.url)location.href=d.url;else throw Error('Billing portal unavailable.')}catch(e){if(window.toast)toast(e.message)}}
  window.startCheckout=startCheckout;window.manageBilling=manageBilling;
  function installApiAuthBridge(){if(window.__psFetchAuthBridge)return;window.__psFetchAuthBridge=true;const original=window.fetch.bind(window);window.fetch=async function(input,init){const url=typeof input==='string'?input:(input&&input.url)||'';if(String(url).startsWith('/api/')&&!(init&&init.__psNoAuth)){await recoverSession();const o=init?Object.assign({},init):{};o.headers=new Headers(o.headers||{});if(token()&&!o.headers.has('Authorization'))o.headers.set('Authorization','Bearer '+token());return original(input,o)}return original(input,init)}}
  function wire(){installApiAuthBridge();const b=document.getElementById('psffaccount');if(b){b.textContent='Account';b.dataset.accountFixed='1';b.onclick=function(){if(window.psFinalShowAccount)window.psFinalShowAccount();else if(token()&&window.openModal)window.openModal('accountModal');else if(window.psFinalShowAuth)window.psFinalShowAuth('login')}}patchPlans()}
  function loadWorkspaceScript(){if(document.getElementById('psWorkspaceScript'))return;const s=document.createElement('script');s.id='psWorkspaceScript';s.src='/workspace-fix.js';document.head.appendChild(s)}
  function loadFrontFix(){if(document.getElementById('psFrontFixScript'))return;const s=document.createElement('script');s.id='psFrontFixScript';s.src='/front-fix.js';document.head.appendChild(s)}
  function loadAuthFlow(){if(document.getElementById('psAuthFlowScript'))return;const s=document.createElement('script');s.id='psAuthFlowScript';s.src='/auth-flow-fix.js';document.head.appendChild(s)}
  function loadInvestigationClickFix(){if(document.getElementById('psInvestigationClickFix'))return;const s=document.createElement('script');s.id='psInvestigationClickFix';s.src='/investigation-click-fix.js';document.body.appendChild(s)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',function(){wire();loadWorkspaceScript();loadFrontFix();loadAuthFlow();recoverSession().then(refreshProfile);setTimeout(loadInvestigationClickFix,0)});else{wire();loadWorkspaceScript();loadFrontFix();loadAuthFlow();recoverSession().then(refreshProfile);setTimeout(loadInvestigationClickFix,0)}
  refreshProfile().then(wire);setInterval(function(){refreshProfile().then(wire)},60000);window.addEventListener('focus',function(){refreshProfile().then(wire)});document.addEventListener('visibilitychange',function(){if(!document.hidden)refreshProfile().then(wire)});
})();
