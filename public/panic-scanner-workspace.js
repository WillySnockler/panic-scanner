/* Deprecated workspace auth shim. The production app uses auth-flow-fix.js + workspace-fix.js.
   Keep this file harmless so an older script reference cannot create a second auth UI. */
(function(){'use strict';window.pswOpen=window.pswOpen||function(key){if(key==='admin'&&window.psFinalShowAccount)window.psFinalShowAccount();};})();
